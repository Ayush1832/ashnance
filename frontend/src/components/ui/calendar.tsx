"use client";

// Calendar stub — react-day-picker not installed. Add `react-day-picker` to deps if calendar is needed.
export function Calendar({ ...props }: React.HTMLAttributes<HTMLDivElement>) {
  return <div {...props} />;
}

import * as React from "react";
export { Calendar as default };
